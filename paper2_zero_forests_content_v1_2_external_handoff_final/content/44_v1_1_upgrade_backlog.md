# v1.1 upgrade backlog

1. Replace the local external-CI dry run with `real_external_run=true`.
2. Compile one Lean/Coq seed theorem and ingest the raw log hash.
3. Add a semantic domain-validator stub.
4. Expand FLRW beyond the selected `nu=0` scalar export.
5. Start manuscript prose only after content freeze artifacts are stable.
