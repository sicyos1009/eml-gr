# v1.3 upgrade backlog

1. Replace the local-preflight record with a hosted `real_external_run=true` record.
2. Ingest at least one no-admit Lean or Coq compile pass.
3. Promote a proof-assistant seed theorem from minimal algebraic cancellation to a rational/Laurent zero leaf.
4. Add a real external artifact hash for the full ZIP release.
5. Add CI matrix coverage for Python versions and optional proof-assistant tools.
6. Add reviewer-facing one-command instructions after repository location is fixed.
