# v1.2 upgrade backlog

1. Replace the dry-run CI record with a real hosted GitHub Actions record.
2. Compile one Lean or Coq seed theorem externally.
3. Move the replay checker behind `emlgr.validation` with tests.
4. Add a richer FLRW component skeleton beyond the selected scalar-export route.
5. Preserve the no-manuscript boundary until evidence posture is stable.
