# External-CI wrapper contract v1.0

The current external-CI record is a local dry-run placeholder:

```text
real_external_run: false
external_provider: local-dry-run-placeholder
```

A future real record must add repository commit SHA, workflow run identifier, raw log SHA256, tool versions, and `real_external_run: true`.
