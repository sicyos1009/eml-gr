# Hosted run evidence dropbox

Place hosted CI records here after a real external run:

```text
external_ci_results.v1_2.github_actions.json
raw_logs/*.log
proof_assistant_compile_report.json
```

Then run the v1.2 validator in `--require-real` mode.
