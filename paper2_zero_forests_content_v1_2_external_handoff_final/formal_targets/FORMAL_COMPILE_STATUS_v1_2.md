# Formal compile status v1.2

```text
compiled_external_run: false in the packaged local preflight
proof_assistant_compile_claimed: false in the packaged local preflight
```

The package includes no-admit seed targets and a compile probe. A hosted or local environment with Lean/Coq/Rocq installed can produce a compile record, but this package does not claim that result by itself.
