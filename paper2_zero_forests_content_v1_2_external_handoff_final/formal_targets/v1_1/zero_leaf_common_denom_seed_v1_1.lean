-- EML-GR Zero Forest v1.1 formal seed target
-- Status: target source only in this artifact; no external compile evidence claimed.

import Mathlib

theorem zero_leaf_self_cancel (x : ℝ) : x - x = 0 := by
  ring
